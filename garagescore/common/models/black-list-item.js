const debug = require('debug')('garagescore:common:models:BlackListItem'); // eslint-disable-line max-len,no-unused-vars

module.exports = function BlackListItemDefinition(BlackListItem) {
  // eslint-disable-line no-unused-vars
};
