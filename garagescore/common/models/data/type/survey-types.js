const Enum = require('../../../lib/util/enum.js');

module.exports = new Enum({
  SURVEY: 'survey',
  SURVEY_UNSATISFIED_FOLLOWUP: 'surveyFollowupUnsatisfied',
  SURVEY_LEAD_FOLLOWUP: 'surveyFollowupLead',
});
