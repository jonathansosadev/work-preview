import React from "react";

const Month = (props) => {
  const { name, days } = props.month;

  return (
    <div className="col">
      <div className="row"></div>
    </div>
  );
};

export default Month;
